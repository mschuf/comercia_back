# Cargado automáticamente por `next build` (la imagen Docker de producción).
# Este archivo SÍ se commitea: solo puede contener variables NEXT_PUBLIC_*
# (terminan en el navegador, son públicas por definición). Nunca secretos.
#
# ⚠️ Cuando haya dominio real, cambiar por https://tudominio.com/api/v1 y hacer
# git push: el pipeline reconstruye la imagen del front con este valor horneado.
# Modo LAN actual: la API vive en el puerto 1001 del servidor.
NEXT_PUBLIC_API_URL="http://172.19.0.140:1001/api/v1"
